# Deltas over the arem-photo-ai conda env (which already has
# torch 2.10.0+cu126, torchvision, numpy, opencv-python-headless, Pillow).
# Install with:
#   conda activate arem-photo-ai
#   pip install -r requirements.txt

# Inference service
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# Object detection (dead-fixture)
ultralytics>=8.3

# Segmentation (reflection)
# Plain segment-anything is on PyPI. MobileSAM is a tiny SAM variant
# (~10x faster) but isn't published to PyPI — install it from the
# upstream repo *only when training* the reflection segmenter:
#   pip install git+https://github.com/ChaoningZhang/MobileSAM.git
# (Requires git on the box; on a vanilla Ubuntu install you may need
# `sudo apt install git` first.) Until that train step, the registry
# stub for "mobile-sam" model_type returns zero-confidence — fine.
segment-anything>=1.0

# Database access for training-data export scripts
psycopg[binary]>=3.2

# R2 (S3-compatible)
boto3>=1.35

# Eval / metrics
scikit-learn>=1.5

# Dev / CLI ergonomics
typer>=0.12
rich>=13.9
